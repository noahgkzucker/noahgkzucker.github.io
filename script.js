document.addEventListener("DOMContentLoaded", () => {
    // JavaScript is not strictly needed here since animations are handled in CSS.
    // This script is included in case additional dynamic behavior is added later.
  });